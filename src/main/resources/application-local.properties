# Local run: app on host, PostgreSQL and Redis on localhost (e.g. docker compose up postgres redis -d)
spring.datasource.url=jdbc:postgresql://localhost:5432/epic_user_service
spring.datasource.username=postgres
spring.datasource.password=postgres

spring.data.redis.host=localhost
spring.data.redis.port=6379
